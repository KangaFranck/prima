      {/* Section 3: Carousel - CONFIGURATION COMPLÈTEMENT NOUVELLE */}
      <section className="py-8 md:py-12 relative overflow-hidden bg-[#E5DDD3]">
        <div className="container mx-auto px-4 relative z-10">
          <div className="text-center mb-6 md:mb-8">
            <h2 className="text-2xl md:text-3xl lg:text-4xl font-neue font-bold mb-4 text-gray-800">DES COMMERCES OUVERTS 7J/7</h2>
          </div>
          
          {/* Vérification des données */}
          {allItems.length === 0 ? (
            <div className="text-center py-8">
              <p className="text-gray-600">Chargement des commerces...</p>
            </div>
          ) : (
            <div className="relative">
              <Swiper
                modules={[Pagination, Autoplay]}
                spaceBetween={8}
                slidesPerView={1.2}
                centeredSlides={false}
                grabCursor={true}
                breakpoints={{
                  480: {
                    slidesPerView: 1.5,
                    spaceBetween: 12,
                  },
                  640: {
                    slidesPerView: 2.2,
                    spaceBetween: 16,
                  },
                  768: {
                    slidesPerView: 3,
                    spaceBetween: 20,
                  },
                  1024: {
                    slidesPerView: 4,
                    spaceBetween: 24,
                  },
                  1280: {
                    slidesPerView: 5,
                    spaceBetween: 28,
                  }
                }}
                pagination={{
                  clickable: true,
                  dynamicBullets: true,
                  bulletClass: 'swiper-pagination-bullet !bg-gray-400 !opacity-50',
                  bulletActiveClass: 'swiper-pagination-bullet-active !bg-amber-600 !opacity-100'
                }}
                autoplay={{
                  delay: 4000,
                  disableOnInteraction: false,
                  pauseOnMouseEnter: true,
                }}
                loop={allItems.length > 3}
                className="!pb-8"
              >
                {allItems.map((item) => (
                  <SwiperSlide key={item.id}>
                    <Link 
                      to={/s/}
                      className="block group"
                    >
                      <div className="bg-white rounded-xl shadow-lg hover:shadow-xl transition-all duration-300 p-3 h-[100px] md:h-[120px] lg:h-[140px] flex items-center justify-center group-hover:scale-105">
                        {(item.logo || item.image) ? (
                          <img
                            src={item.logo || item.image}
                            alt={item.name}
                            className="max-w-full max-h-full object-contain"
                          />
                        ) : (
                          <div className="flex flex-col items-center justify-center text-gray-600">
                            {item.type === 'boutique' && <ShoppingBag className="w-8 h-8 md:w-10 md:h-10 mb-2" />}
                            {item.type === 'restaurant' && <Coffee className="w-8 h-8 md:w-10 md:h-10 mb-2" />}
                            {item.type === 'loisir' && <Dumbbell className="w-8 h-8 md:w-10 md:h-10 mb-2" />}
                            <span className="text-xs md:text-sm font-medium text-center">{item.name}</span>
                          </div>
                        )}
                      </div>
                    </Link>
                  </SwiperSlide>
                ))}
              </Swiper>
            </div>
          )}
        </div>
      </section>
